/* The LineRange class defines a range of lines in a document with a starting and ending line number. */
class LineRange {
  int? lineStart;
  int? lineEnd;

  LineRange(this.lineStart, this.lineEnd);
}
